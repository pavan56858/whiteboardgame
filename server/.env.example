PORT=4000
JWT_SECRET=Love
CLIENT_ORIGIN=http://localhost:5173

# Optional — enables the "✨ AI Diagram" button. Get a key at
# https://console.anthropic.com. Without this set, the app still works;
# that one button will just show a clear error instead of a diagram.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
